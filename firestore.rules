rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      // Cho phép người dùng đọc/ghi dữ liệu của chính mình sau khi đã đăng nhập
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
